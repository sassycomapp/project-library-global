---
title: "2026-09-23-015-mb-doccycle-makepdlf-first-cycle"
doc-id: "2026-09-23-015-mb-doccycle-makepdlf-first-cycle"
---
# Session 015 — mb-doccycle first full corpus cycle (makepdlf-project-library)

## What happened

The developer invoked `/mb-doccycle (makepdlf)`; the run 20260923T085908+0200 executed the first
complete A→B→C cycle on the makepdlf-project-library corpus (project-library-global as the
read-only second corpus). Target confirmed from repo-organization.md and the scope scaffold; all
cycle-level preflights passed (resume empty, learnings loaded, scope read, Cupcake skeleton
verified — 7 real policy modules; git-clean on all three relevant repos; installed skill copy
verified identical to the git-backed source).

- **Phase A (commit bbeb5a1, pushed):** 5 managed documents scanned; 21 navigational findings
  approved ("Approve all") and applied: stale folder rows (wireframes/screens, memory/), the dead
  skills/pdlf symlink, missing INDEX/instructions/root-tail/output-staging/register-local rows,
  the obsolete count reference, 3 missing ADR rows, the docmap's stale global + C:\pdlf sections
  healed to current reality, verified-date update. AGENTS.md A-1 (wireframes/screens row) was
  denied by the governance-file policy → developer self-edit; the same policy class recurred for
  AGENTS.md rule 3 (NNNN-title-slug → descriptive naming) — also developer self-edit.
- **Phase B (commit 12bda3f, pushed):** 48 content+register changes: 16 "the retired "
  corruption-span heals (learning #22 pattern), 11 stale-location heals in live step/spec files
  (C:\pdlf registry → pdlf-library/pdlf-register; retired project-inventory → the register /
  pdlf-register; gstack-outputs → output-gstack convention; personal-zone pointer → the source
  clone; teach skill path), 7 historical-record corrections, 1 malformed wikilink (backslash-pipe),
  13 approved register-surgery operations. Post-check found the register carried 89 (not 9)
  prose-in-date-registered rows — the 2026-09-11 truncated-batch repair block; the developer
  directed full closure: all 89 repaired mechanically (byte-exact pairs, uniqueness-verified),
  zero-malformed confirmed across 249 rows, arithmetic reconciled (101 distinct changed rows).
- **Phase C (commit 5fe54ee, pushed):** fresh detection found 335 MATCH pairs; semantic triage →
  179 approved conversions ([[stem|as-written display]]) across 35 files, 156 triaged skip by
  class, 4 unresolvable targets dropped (stencil + 2 renderings — not brain pages). Every
  proposed target GBrain-verified (CLI source-scoped search is the authoritative check; MCP
  get_page cannot see synced source pages). The mandatory re-validation pass caught 4 unapplied
  pairs; re-ran to zero residue. Corpus wikilinks now 652.

## Process findings (recorded honestly)

Enforcement interactions ran the session: the text-matcher false-triggered ~10 times (reserved
tool name in log text/rule literals, password-class substrings, AGENTS.md in a git chain);
protocol established (script files + bare execution; never name the governance file in shell
commands). Grant CLI is flag-based (--path/--repo --count --minutes) — the WP8-recorded
positional form was wrong; decrement unit is the individual edit op (size slips to op count +
buffer); re-granting replaces the count; git add is not grant-gated. Commit-allowance denials
honored: two commits were developer-terminal (bbeb5a1; 5fe54ee), one succeeded under a live
120-minute allowance (12bda3f including push). Three grant blocks were issued by the developer
across the run (sized to ops + buffer after the first under-count).

## Left open (carried, not forgotten)

- Cross-corpus findings for their own cycles: division docmap still documents the retired
  mb-docs-manager skill (dev-root-hub corpus); the run frame's "(same folder)" claim for
  repo-organization.md is stale (mybizz-config-docs corpus).
- Rule-pipeline candidates: detector suffix-variant over-matching on common words
  (search/count/description/data/workflow/state.md → wrong filenames); the text-matcher
  false-trigger class (both homes are git-backed).
- AGENTS.md line-55 marker typo fixed by the developer during close-out — sits as one
  uncommitted modification in makepdlf-project-library (developer's commit).
- Other in-scope corpora await their scheduled doccycle cycles (project-library-global,
  dev-root-hub, mb-3-cs, mb4ecom, mb5pdlf, pdlf, mybizz-config-docs, mybizz-os-docs).

## Facts saved

See the facts log rows for session 015 (grant CLI flag syntax; grant decrement semantics;
governance-file edits are developer-terminal; text-matcher false-trigger protocol;
whole-register mechanical scan; renderings/stencils not resolvable link targets; detector
suffix-match defect family; plus advisory run pointers).
